<?xml version="1.0" encoding="utf-8"?>
<xml>
	<files id="dr_libs-files" dir="${DR_LIBS_OVERRIDE_DIRECTORY}" tags="optim-std">
		<compilerflag value="-I${DR_LIBS_DIRECTORY}" />

		<compilerflag value="-DDRMP3_DLL" unless="emscripten" />
		<compilerflag value="-DDRWAV_DLL" unless="emscripten" />
		<compilerflag value="-DDRFLAC_DLL" unless="emscripten" />

		<file name="dr_impl.cpp" />
	</files>
</xml>
